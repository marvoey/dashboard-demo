---
interface Product {
  name: string;
  enrolled: number;
  premium: string;
}
interface Props {
  products: Product[];
}
const { products } = Astro.props;
---

<div class="comp comp-table span-2">
  <div class="comp-bar">
    <div class="comp-bar-left">
      <a href="/components/data/enrollment-table" target="_blank" class="comp-explain-link">🗄 Data Table ↗</a>
      <span style="opacity:0.4;">·</span>
      <span style="font-weight:500; text-transform:none; letter-spacing:0;">SQL Server</span>
    </div>
    <span class="data-source">🗄 SQL Server</span>
  </div>
  <div class="comp-body-flush">
    <div style="padding: 14px 16px; border-bottom: 1px solid var(--border);">
      <span class="card-title">Enrollment by Product</span>
    </div>
    <table>
      <thead>
        <tr>
          <th>Product</th>
          <th>Enrolled</th>
          <th>Monthly Premium</th>
        </tr>
      </thead>
      <tbody>
        {products.map(p => (
          <tr>
            <td style="font-weight: 500;">{p.name}</td>
            <td>{p.enrolled} <span style="color: var(--text-muted); font-size: 0.75rem;">employees</span></td>
            <td style="font-weight: 500;">{p.premium}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</div>
